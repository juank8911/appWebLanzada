export class Member {
    // constructor(public id: string, public email: string, public admin: string, public password: string, public tokenpsh: string,
    //             public salt: number, public salt_contra: number, public locked: number, public updateAT: string ) { }

    constructor (public email: string, public pssw: string) { }
}

export class Provedor {
    constructor(public id: string, public nit: number, public nombre: string, public correo: string, public direccion: string,
        public telefono: string, public avatar: string, public whatsapp: string, public link: string, public video: string,
        public descripcion: string, public members_id: string) {}
}
